# Test runner image. Includes devDependencies and runs `npm test` against the
# postgres service defined in docker-compose.yml.
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
ENV NODE_ENV=test
CMD ["sh", "-c", "node scripts/migrate.js && npm test"]
